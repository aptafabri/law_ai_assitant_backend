
services:
  adaletgpt_backend_web:
    build: .
    image: adaletgpt-backend:latest
    container_name: adaletgpt_backend_web
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: "always"
    environment: PYTHONPATH=/app